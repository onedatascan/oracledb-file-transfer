### Setup
- Requires Python 3.7+
- Install Make
- Install Docker
- Create and activate virtual environment
```
make create-venv
. .venv/bin/activate
pip install -r dev-requirements.txt
```

### Run tests
```
make test
```

## Build
```
make build
```